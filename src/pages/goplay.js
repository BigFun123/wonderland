import React, { useState } from 'react';

function GoPlay(options) {

    return (
        <div className='flexbox'>            
            <button onClick={search}>Register</button>
            <button onClick={search}>Login</button>
        </div>
    );
}

export default GoPlay;